<?xml version="1.0" encoding="utf-8"?>
<!-- res/values/themes.xml -->
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.TuAplicacion" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- ... otros atributos de tu tema ... -->


        <!-- Para barras del sistema transparentes -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <!-- Para Android API 27+ puedes controlar el divisor de la barra de navegación -->
        <item name="android:navigationBarDividerColor" tools:targetApi="27">@android:color/transparent</item>

        <!-- Para controlar el color de los iconos de la barra de estado y navegación (claro/oscuro) -->
        <!-- WindowInsetsControllerCompat se encarga de esto mejor programáticamente -->
    </style>

</resources>
